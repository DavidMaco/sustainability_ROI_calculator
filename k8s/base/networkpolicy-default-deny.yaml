apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sustainability-roi-default-deny
  namespace: sustainability-roi
spec:
  podSelector:
    matchLabels:
      app: sustainability-roi
  policyTypes:
    - Ingress
    - Egress
